/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The playground is a single-page tool; no image optimisation or telemetry.
  poweredByHeader: false,
};

export default nextConfig;
